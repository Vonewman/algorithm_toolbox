#include <string>
#include <vector>
#include <algorithm>

using namespace std;

int findMax(const vector<int>& arr);

int main() {
    return 0;
}

int findMax(const vector<int>& arr) {
    vector<int> b;
    while (arr.size() != 0) {
	
    }
}
